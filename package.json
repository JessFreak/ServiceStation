{
  "name": "service-station-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "back",
    "front"
  ],
  "scripts": {
    "test": "npm run generate -w back && npm test -w back"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "jest-util": "^29.5.0",
    "expect": "^29.5.0",
    "slash": "3.0.0"
  }
}